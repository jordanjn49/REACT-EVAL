import fetch from "node-fetch";
import { useEffect, useState } from "react";


function Weather() {


  
  return <>WeatherAPI</>;
  // return forecast?.map((item) => (
  //   <div key={item.dt}>
  //     {item.dt_txt}: {item.main.temp}°F
  //   </div>
  // ));
}

export default Weather;
